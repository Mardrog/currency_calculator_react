import { StyledButton } from "./styled";

const Buttons = () => {

    return (
        <div>
            <StyledButton >Przelicz</StyledButton>
            <StyledButton >Wyczyść</StyledButton>
        </div >
    )
};

export { Buttons };
